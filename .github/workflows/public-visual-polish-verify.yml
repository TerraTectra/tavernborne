name: Public Visual Polish v1 verify

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium

      - name: Verify public GitHub Pages build
        env:
          TEST_URL: https://terratectra.github.io/tavernborne/
        run: |
          for attempt in {1..6}; do
            if node scripts/public-visual-polish-verify.mjs; then
              exit 0
            fi
            echo "Public Visual Polish build not ready; retry $attempt/6"
            sleep 30
          done
          exit 1

      - name: Upload public verification
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: public-visual-polish-verification
          path: |
            public-visual-polish-diagnostics.json
            public-visual-polish-camp.png
            public-visual-polish-dungeon.png
          if-no-files-found: ignore
